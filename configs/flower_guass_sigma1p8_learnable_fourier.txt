# ========== dataset & training ==========
expname      = flower_gauss_sigma1p8_aligned_learnable_fourier
basedir      = ./logs
datadir      = ./nerf_llff_data/flower
dataset_type = llff

factor   = 8
llffhold = 8
N_rand   = 1024

N_samples    = 64
N_importance = 64
N_iters      = 30000

# ========== NeRF 网络结构 ==========
netwidth = 256
netdepth = 8

# ========== 位置编码：Gaussian Fourier Features ==========
i_embed  = 0
multires = 0          # 对 Gaussian 来说这里无所谓（不使用 log-bands）

pe_type      = gaussian
pe_num_feats = 30
pe_sigma     = 1.8

# 这里先保持 Gaussian 特征是“固定随机”的，不可学习，和原始设计一致
learnable_pe        = True
pe_learnable_freqs  = True
learnable_pe_phase  = False
pe_init_scale       = 1.0
pe_lr_scale         = 1.0

# ========== 视角编码 & 噪声 ==========
use_viewdirs   = True
multires_views = 4          # 视角仍然使用标准 NeRF 的 log-bands
raw_noise_std  = 1e-2       # 与另外两个实验保持一致

# ========== 优化器设置 ==========
lrate       = 5e-4
grad_clip   = 1.0

# ========== Weights & Biases ==========
use_wandb      = True
wandb_project  = Nerf_IDL_gauss
wandb_run_name = flower_gauss_sigma1p8_aligned_learnable_fourier